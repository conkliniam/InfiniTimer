<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InfiniTimer.TimersPage"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title="Saved Timers">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="headerRow" Height="*"/>
            <RowDefinition x:Name="hiddenRow" Height="0"/>
            <RowDefinition x:Name="listRow" Height="11*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="10" Clicked="NewTimerClicked" Text="New Timer"/>
            <ImageButton Grid.Column="1"
                         Source="{AppThemeBinding Dark=three_dots_vertical_light.png, Light=three_dots_vertical_dark.png}"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         Padding="5"
                         Clicked="OptionsIconClicked"/>
        </Grid>
        <Grid Grid.Row="1"
              BackgroundColor="{StaticResource Primary}"
              IsVisible="false"
              x:Name="actionButtons">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0"
                    Grid.Column="0"
                    Text="Select All"
                    Margin="2"
                    Clicked="SelectAllClicked"
                    IsEnabled="{Binding AnyUnselected}"/>
            <Button Grid.Row="0"
                    Grid.Column="1"
                    Text="Unselect All"
                    Margin="2"
                    Clicked="UnselectAllClicked"
                    IsEnabled="{Binding AnySelected}"/>
            <Button Grid.Row="1"
                    Grid.Column="0"
                    Text="Stage Selected"
                    Clicked="StageSelectedClicked"
                    IsEnabled="{Binding AnyUnstagedSelected}"
                    Margin="2"/>
            <Button Grid.Row="1"
                    Grid.Column="1"
                    Text="Unstage Selected"
                    Clicked="UnstageSelectedClicked"
                    IsEnabled="{Binding AnyStagedSelected}"
                    Margin="2"/>
            <Button Grid.Row="2"
                    Grid.Column="0"
                    Text="Delete Selected"
                    Clicked="DeleteSelectedClicked"
                    IsEnabled="{Binding AnySelected}"
                    ImageSource="{AppThemeBinding Dark=trash_fill_dark.png, Light=trash_fill_light.png}"
                    Margin="2">
            </Button>
            <Button Grid.Row="2"
                    Grid.Column="1"
                    Text="Delete All"
                    Clicked="DeleteClicked"
                    ImageSource="{AppThemeBinding Dark=trash_fill_dark.png, Light=trash_fill_light.png}"
                    IsEnabled="{Binding AnyTimers}"
                    Margin="2"/>
            <Button Grid.Row="3"
                    Grid.Column="0"
                    Text="Add Defaults"
                    Clicked="AddDefaultClicked"
                    Margin="2"/>
        </Grid>
        <ScrollView Grid.Row="2" ZIndex="0">
            <VerticalStackLayout>
                <ListView x:Name="listTimers"
                          BackgroundColor="Transparent"
                          ItemsSource="{Binding TimerModels}"
                          ItemSelected="ListTimersItemSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="5*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"/>
                                    <Label Grid.Column="1" Margin="10" Text="{Binding Name}"></Label>
                                    <ImageButton Grid.Column="2"
                                                 Source="{AppThemeBinding Dark=floppy_fill_light.png, Light=floppy_fill_dark.png}"
                                                 Clicked="SaveIconClicked"
                                                 IsVisible="{Binding IsDirty}"
                                                 Padding="5"/>
                                    <ImageButton Grid.Column="3"
                                                 Source="{AppThemeBinding Dark=arrow_counterclockwise_light.png, Light=arrow_counterclockwise_dark.png}"
                                                 Clicked="ResetIconClicked"
                                                 IsVisible="{Binding IsDirty}"
                                                 Padding="5"/>
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>